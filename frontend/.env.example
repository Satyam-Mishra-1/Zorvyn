# Local dev: leave empty to use Vite proxy (vite.config.ts) → http://127.0.0.1:4000
# Vercel production: set this in the Vercel dashboard to your Render API URL (no trailing slash)
# Example: VITE_API_URL=https://finance-api-xxxx.onrender.com
VITE_API_URL=
